
Please ensure Node.JS has been installed in your system.

Follow the below mentioned steps:

  1> Clone the repository in your system.
  
  2> Open Command Prompt in that particular projet directory.
  
  3> Run "npm install" command to install the node modules required to run the project.
  
  4> Run "npm start" command to run the project.
